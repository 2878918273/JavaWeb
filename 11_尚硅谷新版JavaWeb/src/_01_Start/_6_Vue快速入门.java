package _01_Start;

/*
今天的内容：
1.Vue
    1）{{}} - 相当于innerText
    2）v-bind:attr 绑定属性值。例如，v-bind:value - 绑定value值
        简写： value
    3）v-model 双向绑定
    4）v-if，v-else，v-show
       v-if和v-else之间不能有其它的节点
       v-show是通过样式表display来控制节点是否显示
    5）v-for迭代
       v-for="fruit in fruitList"
    6）v-on 绑定事件
    7）其它：
        trim：去除首尾空格，split(),join()
        watch表示侦听属性
        生命周期

*/

/**
 * @author wx
 * @create 2023-05-31 17:09
 */
public class _6_Vue快速入门 {
}
